

export interface Comments {
    comments: string;
    path: string;
    element: JSX.Element;
  }

export interface Task {
    name: string,
    points: number,
    difficulty: string,
    status: string,
};